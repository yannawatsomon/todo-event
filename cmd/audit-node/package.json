{
  "name": "audit-node",
  "version": "1.0.0",
  "description": "Node.js TypeScript replacement for cmd/audit — subscribes to RabbitMQ and pushes events to Loki",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc"

  },
  "dependencies": {
    "amqplib": "^0.10.4"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
